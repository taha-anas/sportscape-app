server:
  port: 8087

spring:
  application:
    name: booking-service
  #  docker:
  #    compose:
  #      enabled : true
  datasource:
    url: jdbc:postgresql://sportscape-postgres:5432/sportscape
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false


eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
    fetch-registry: true
    register-with-eureka: true